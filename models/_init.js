/**
 * Init file for create default collections and indexes in database
 * Works only with model with next extantion '*.coffee'
 */

//          require('coffee-script');
// var fs = require("fs"),
//     db = require("../db");

// fs.readdirSync(__dirname).forEach(function(file) {

//   // IF '*.coffee'
//   if (file.match(/\.coffee/)) { 	
// 	  if (require("./" + file)) {
// 		  console.log("Loaded model: " + file);
// 	  }
//   }
// });

// db.connection.close();