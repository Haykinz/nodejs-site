var ftp = require('../');
var assert = require('assert');
var callback = false;

ftp.head('ftp://0000-2346:2346UMG@ftp.detroittrading.net/dtcinventory.zip', function (err, size) {
	callback = true;
	// assert.ifError(err);
	// assert.deepEqual(size, '4');
	console.log(size);
});

process.on('exit', function () {
	assert.ok(callback);
});
